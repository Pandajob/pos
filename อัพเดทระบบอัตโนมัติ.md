# อัพเดทระบบอัตโนมัติ

เลิกก๊อปไฟล์ไปเครื่องร้านทีละไฟล์ เครื่องร้านจะโหลดอัพเดทเองจากอินเทอร์เน็ต
และ **ตรวจ SHA-256 ทุกไฟล์** ก่อนติดตั้ง — ไม่ต้องเดาอีกว่าไฟล์ครบไหม เพี้ยนไหม

---

## ภาพรวม

```
เครื่องพัฒนา                          GitHub                      เครื่องร้าน
─────────────                        ──────                      ──────────
build-release.ps1  ──อัปโหลด──>  latest.json  <──เช็คทุกคืน──  update-pos.ps1
       │                          pos-update-x.zip  ──โหลด──>        │
       └─ ใส่ SHA-256 ของ                                            ├─ ตรวจ checksum ทั้งก้อน
          ทุกไฟล์ลง manifest                                        ├─ ตรวจ checksum ทีละไฟล์
                                                                     ├─ สำรองของเดิม
                                                                     ├─ ติดตั้ง
                                                                     └─ รัน migration
```

**ไม่แตะของประจำเครื่องเด็ดขาด:** `.env` · `writable\` · `public\uploads\`
(ตั้งค่าร้าน รูป QR ข้อมูลขาย เซสชัน — อยู่ครบเหมือนเดิมทุกครั้ง)

---

## ตั้งค่าไว้แล้ว (ไม่ต้องทำซ้ำ)

| อะไร | ค่า |
|---|---|
| repo | https://github.com/Pandajob/pos |
| ไฟล์ที่เครื่องร้านมาเช็ค | `https://raw.githubusercontent.com/Pandajob/pos/main/latest.json` |
| ที่เก็บก้อนอัพเดท | โฟลเดอร์ `releases/` ในรีโป (เก็บ 3 รุ่นล่าสุด) |
| ตั้งค่าที่เครื่องร้าน | `tools\update-source.txt` (มากับก้อนอัพเดทแล้ว) |

เหลืออย่างเดียวที่ต้องทำที่เครื่องร้าน — สั่งครั้งเดียวให้มันเช็คเองทุกคืนตี 3:

```powershell
cd C:\xampp\htdocs\POS
.\tools\update-pos.ps1 -InstallSchedule
```

---

## เวลาจะปล่อยอัพเดท (ทำที่เครื่องพัฒนา)

```powershell
.\tools\build-release.ps1 -Version 2026.08.22.1 -Notes "แก้ใบเสร็จ A4"
```

```bash
git add -A && git commit -m "release 2026.08.22.1" && git push
```

**จบแค่นี้** — ไม่ต้องเข้าหน้าเว็บ GitHub ไม่ต้องอัปโหลดไฟล์เอง
คืนนั้นเครื่องร้านเช็คเจอแล้วอัพเดทเอง

สคริปต์ทำให้อัตโนมัติทั้งหมด:
- อ่านชื่อ repo จาก `git remote` เอง (ไม่ต้องพิมพ์ URL ให้ผิด)
- ก๊อป zip เข้า `releases/` แล้วชี้ลิงก์ให้ถูก
- เขียน `latest.json` ที่ root ของรีโป
- ลบก้อนเก่าเหลือ 3 รุ่นล่าสุด ไม่ให้รีโปบวม

> ถ้าไม่ใส่ `-Version` จะตั้งเลขจากวันที่ให้เอง (`ปี.เดือน.วัน.ลำดับ`)
>
> ถ้าวันหนึ่งไฟล์ใหญ่จนไม่อยากเก็บในรีโป เปลี่ยนไปใช้ GitHub Releases ได้ด้วย
> `-ZipUrlBase "https://github.com/Pandajob/pos/releases/download"` (แล้วต้องอัปโหลด zip เองที่หน้าเว็บ)

---

## คำสั่งที่เครื่องร้าน

| สั่งแบบนี้ | ได้อะไร |
|---|---|
| ดับเบิลคลิก `tools\update-pos.bat` | เช็คแล้วอัพเดทให้เลย |
| `.\tools\update-pos.ps1 -Check` | บอกว่ามีเวอร์ชันใหม่ไหม ไม่ติดตั้ง |
| `.\tools\update-pos.ps1 -Force` | **ซ่อมไฟล์** — ติดตั้งทับใหม่ทั้งชุดแม้เวอร์ชันเท่าเดิม ใช้ตอนสงสัยว่าไฟล์หายหรือเพี้ยน |
| `.\tools\update-pos.ps1 -InstallSchedule` | ตั้งให้อัพเดทเองทุกคืนตี 3 |
| `.\tools\update-pos.ps1 -RemoveSchedule` | เลิกอัพเดทอัตโนมัติ |
| `.\tools\update-pos.ps1 -Package "D:\pos-update-x.zip"` | ติดตั้งจากไฟล์ในเครื่อง/USB — **ไม่ต้องมีเน็ต** ยังตรวจ checksum ทุกไฟล์เหมือนเดิม |

---

## ครั้งแรก: ย้ายของขึ้นเครื่องร้าน

ครั้งนี้ครั้งเดียวที่ต้องหอบไฟล์ไปเอง เพราะเครื่องร้านยังไม่มีตัว updater
หลังจากนี้มันอัพเดทเองหมด

**1.** ที่เครื่องพัฒนา รัน `.\tools\build-release.ps1` แล้วก๊อป **2 ไฟล์** ใส่ USB

- `dist\pos-update-<version>.zip`
- `tools\update-pos.ps1`

**2.** เสียบ USB ที่เครื่องร้าน เปิด PowerShell แล้วสั่ง (สมมติ USB เป็นไดรฟ์ D:)

```powershell
powershell -ExecutionPolicy Bypass -File D:\update-pos.ps1 -PosRoot "C:\xampp\htdocs\POS" -Package "D:\pos-update-2026.08.21.2.zip"
```

จบ — ตัว updater จะติดตั้งไฟล์ทั้งหมด (รวมถึงตัวมันเองลงใน `tools\`)
ตรวจ checksum ทุกไฟล์ สำรองของเดิม แล้วรัน migration ให้

> ไม่ต้องไล่ก๊อปทีละไฟล์ และไม่ต้องเดาว่าครบไหม
> บรรทัด **"ครบ N ไฟล์ ตรงทุกไฟล์"** คือคำตอบ

**3.** จากนั้นตั้งอัพเดทอัตโนมัติ (ต้องตั้ง URL ใน `tools\update-source.txt` ก่อน)

```powershell
cd C:\xampp\htdocs\POS
.\tools\update-pos.ps1 -InstallSchedule
```

---

## ถ้าอัพเดทแล้วพัง

ไฟล์เดิมถูกสำรองเป็น zip ไว้ทุกครั้งก่อนทับ อยู่ในโฟลเดอร์ `backup\`
(เก็บ 5 ชุดล่าสุด ชื่อไฟล์บอกเวอร์ชันเดิมกับวันเวลา)

แตก zip ทับกลับที่โฟลเดอร์ POS ก็กลับไปเหมือนเดิม

ถ้าล้มเหลว **ระหว่าง** ติดตั้ง สคริปต์จะคืนค่าให้เองอัตโนมัติ
และถ้าตรวจ checksum ไม่ผ่านตั้งแต่แรก จะยกเลิกโดย**ไม่แตะไฟล์เดิมเลยสักไฟล์**

---

## สิ่งที่ระบบนี้ป้องกันให้

| ปัญหา | ระบบทำอะไร |
|---|---|
| ก๊อปไฟล์ไม่ครบ | ตรวจ SHA-256 ทีละไฟล์เทียบกับ manifest — ขาดแม้ไฟล์เดียวคือยกเลิก |
| ไฟล์เสียระหว่างโหลด | ตรวจ SHA-256 ของทั้งก้อน zip ก่อนแตก |
| ก๊อปทับไฟล์ตั้งค่าร้าน | `.env` / `writable` / `uploads` ไม่อยู่ในก้อนอัพเดทตั้งแต่ต้น |
| ลืมกดอัพเดทฐานข้อมูล | รัน `spark migrate` ให้อัตโนมัติหลังติดตั้ง |
| อัพเดทแล้วพัง ย้อนไม่ได้ | สำรองอัตโนมัติทุกครั้ง + คืนค่าเองถ้าล้มระหว่างทาง |
| ไม่รู้ว่าเครื่องร้านเวอร์ชันอะไร | ไฟล์ `VERSION` ที่โฟลเดอร์ POS |

---

## ข้อจำกัด

- **โปรแกรมพิมพ์ (`local-agent\`) ไม่รวมอยู่ในอัพเดทนี้** — มันเป็น Electron ที่ build เป็น .exe แยก
  ทับตอนกำลังรันอยู่ไม่ได้ ถ้าแก้ตัวนั้นต้องเอาไปติดตั้งเองเหมือนเดิม
- ต้องมีเน็ตที่เครื่องร้าน (เฉพาะตอนอัพเดท — ตัวเว็บ POS ใช้งานออฟไลน์ได้ปกติ)
- `latest.json` ต้องอยู่ที่ URL คงที่ ไม่งั้นเครื่องร้านหาไม่เจอ
