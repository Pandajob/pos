# ปรับเครื่องร้านให้เร็วขึ้น (เครื่องสเปคน้อย)

ส่วนที่แก้ในโค้ดแล้ว copy ไปทับได้เลย ส่วนในเอกสารนี้คือสิ่งที่ต้อง **ตั้งค่าที่ตัวเครื่อง** — ทำครั้งเดียวจบ

## ทางลัด: ให้สคริปต์ทำให้ทั้งหมด

ไม่ต้องไล่แก้เองทีละข้อ — ที่เครื่องร้าน **คลิกขวาที่ `tools\tune-pos-machine.bat` แล้วเลือก Run as administrator**

สคริปต์จะทำข้อ 1-5 ให้ครบ สำรองไฟล์เดิมไว้ทุกไฟล์ (`.bak-วันเวลา`) และรันซ้ำได้ไม่พัง

```powershell
# ดูก่อนว่าจะแก้อะไรบ้าง โดยยังไม่แก้จริง
.\tools\tune-pos-machine.ps1 -DryRun

# แก้จริง แล้วรีสตาร์ท Apache/MySQL ให้เลย
.\tools\tune-pos-machine.ps1 -Restart
```

| ตัวเลือก | ทำอะไร |
|---|---|
| `-DryRun` | บอกว่าจะแก้อะไร แต่ยังไม่แก้จริง |
| `-Restart` | รีสตาร์ท Apache/MySQL ให้หลังแก้เสร็จ (ถ้าติดตั้งเป็น Windows service) |
| `-ClearSessions` | ล้าง session เก่าด้วย — ทุกคนต้อง login ใหม่ 1 ครั้ง |
| `-FastDiskWrites` | เร่ง MySQL เพิ่ม (ดูข้อแลกเปลี่ยนในข้อ 5) |
| `-SkipMysqlTuning` | ไม่แตะ my.ini |
| `-XamppRoot` / `-PosRoot` | ถ้า XAMPP ไม่ได้อยู่ที่ `C:\xampp` |

เนื้อหาที่เหลือคือรายละเอียดว่าสคริปต์ไปแก้อะไรบ้าง — ไว้อ่านทำความเข้าใจ หรือแก้มือเองถ้าอยากทำเอง
เรียงตามผลลัพธ์ที่ได้ มากไปน้อย

---

## 1. เปิด OPcache ใน PHP — ผลมากที่สุด

ตอนนี้ปิดอยู่ แปลว่า **ทุกครั้งที่เปิดหน้าเว็บ PHP ต้องแปลไฟล์โค้ดใหม่ทั้งหมดกว่า 150 ไฟล์**
เปิด OPcache แล้ว PHP จะจำผลการแปลไว้ในหน่วยความจำ ไม่ต้องทำซ้ำ — เร็วขึ้นราว 2-4 เท่า

แก้ไฟล์ `C:\xampp\php\php.ini` หาบรรทัด `;zend_extension=opcache` (ราวบรรทัด 964)
เอา `;` ข้างหน้าออก แล้ว **เพิ่มท้ายไฟล์** ตามนี้:

```ini
[opcache]
opcache.enable=1
opcache.enable_cli=0
opcache.memory_consumption=96
opcache.interned_strings_buffer=8
opcache.max_accelerated_files=10000
opcache.validate_timestamps=1
opcache.revalidate_freq=60
opcache.save_comments=1
```

เสร็จแล้ว restart Apache

> `revalidate_freq=60` = ถ้า copy ไฟล์ใหม่ไปทับ ต้องรอไม่เกิน 1 นาทีถึงจะมีผล
> ถ้าอยากให้มีผลทันทีหลัง copy ให้ restart Apache

---

## 2. เปลี่ยนเป็นโหมด production

แก้ไฟล์ `.env` ที่เครื่องร้าน บรรทัดแรก:

```
CI_ENVIRONMENT = production
```

ตอนนี้เป็น `development` ซึ่งทำให้ CodeIgniter:
- เขียนไฟล์ debug ลง `writable/debugbar/` **ทุกครั้งที่เปิดหน้าเว็บ** (ที่เครื่องนี้กองอยู่ 1.9 MB แล้ว)
- แอบเก็บสถิติทุกคำสั่ง SQL ทุก view ที่โหลด
- ยัด JavaScript แถบ debug เข้าไปในทุกหน้า

โหมด production ตัดทั้งหมดนี้ทิ้ง

**ลบของเก่าที่กองไว้ด้วย** (ปลอดภัย ไม่กระทบข้อมูลขาย):

```bash
del /q C:\xampp\htdocs\POS\writable\debugbar\*
del /q C:\xampp\htdocs\POS\writable\session\*
```

> ลบ session = ทุกคนต้อง login ใหม่รอบเดียว

---

## 3. กดปุ่ม "อัพเดทฐานข้อมูล" ในหน้าตั้งค่า

รอบนี้มี migration ใหม่ที่เพิ่ม index ให้ตาราง `sales` และ `sale_items`

เดิมตาราง `sales` มีแค่ PRIMARY กับเลขบิล — เวลาเปิดแดชบอร์ด ดูรายงาน หรือแม้แต่โหลดหน้าธรรมดา
(ตัวเลข badge "ค้างชำระ" ที่แถบเมนูทำงานทุกหน้า) MySQL ต้องไล่อ่านบิลทั้งตาราง
**ยิ่งขายมานานยิ่งช้าลงเรื่อย ๆ** — ข้อนี้แก้ตรงจุดนั้น

---

## 4. เปิด 3 โมดูลของ Apache

เปิดแล้วเว็บจะบีบอัดไฟล์ก่อนส่ง (CSS 232KB เหลือ 31KB — วัดจริงแล้ว)
และบอกเบราว์เซอร์ให้เก็บไฟล์ไว้ใช้ซ้ำ

แก้ `C:\xampp\apache\conf\httpd.conf` เอา `#` ข้างหน้า 3 บรรทัดนี้ออก:

```apache
LoadModule deflate_module modules/mod_deflate.so
LoadModule expires_module modules/mod_expires.so
LoadModule filter_module modules/mod_filter.so
```

(`mod_headers` เปิดอยู่แล้วโดยปกติ) เสร็จแล้ว restart Apache

> ⚠️ ต้องเปิด **mod_filter** ด้วย ไม่ใช่แค่ mod_deflate ไม่งั้น Apache จะขึ้น error 500 ทุกหน้า
> (กฎใน `.htaccess` เขียนกันไว้แล้ว ถ้าไม่ได้เปิดก็แค่ข้ามไป ไม่พัง — แต่ก็จะไม่ได้ gzip)

เครื่องนี้ (เครื่องพัฒนา) เปิดให้แล้ว สำรอง httpd.conf เดิมไว้ที่ `httpd.conf.bak-YYYYMMDD`

---

## 5. ตั้งค่า MySQL (ถ้ายังรู้สึกหนัก)

แก้ `C:\xampp\mysql\bin\my.ini` ในส่วน `[mysqld]`:

```ini
innodb_buffer_pool_size=256M
innodb_flush_log_at_trx_commit=2
innodb_flush_method=normal
max_connections=30
performance_schema=OFF
table_open_cache=400
```

- `innodb_buffer_pool_size` = แรมที่ MySQL ใช้เก็บข้อมูลไว้ตอบเร็ว ๆ
  ถ้าเครื่องมีแรม 4GB ใช้ 256M / แรม 8GB ใช้ 512M
- `performance_schema=OFF` ประหยัดแรมได้ราว 100-200MB ทันที
- `innodb_flush_log_at_trx_commit=2` เขียนดิสก์น้อยลงมาก (เร็วขึ้นชัดถ้าเป็นฮาร์ดดิสก์จานหมุน)
  แลกกับว่าถ้าไฟดับกะทันหันอาจเสียรายการวินาทีสุดท้าย — ถ้ารับไม่ได้ให้ข้ามบรรทัดนี้

restart MySQL หลังแก้

---

## 6. เบราว์เซอร์ที่เครื่องร้าน

- เปิด Chrome/Edge แบบ **kiosk** เฉพาะหน้า POS ไม่ต้องเปิดแท็บอื่นค้างไว้ (แต่ละแท็บกินแรม 100-300MB)
- ปิดส่วนขยาย (extension) ที่ไม่ได้ใช้ทั้งหมด
- ถ้ามีจอลูกค้า (customer display) เปิดไว้ — ตอนนี้โค้ดปรับให้ถามข้อมูลถี่เฉพาะตอนกำลังขาย
  ร้านว่างจะถามห่างลงเอง และถ้าจอถูกซ่อน/ย่อจะหยุดถามเลย

---

## สิ่งที่แก้ในโค้ดให้แล้ว (มากับไฟล์ที่ copy ไป)

| แก้อะไร | ได้อะไร |
|---|---|
| ย้าย Bootstrap / Chart.js / JsBarcode / html2canvas มาไว้ในเครื่อง (`public/assets/`) | ไม่ต้องรอโหลดจากอินเทอร์เน็ตทุกหน้า **และใช้งานได้แม้เน็ตหลุด** |
| เปิด gzip + ตั้งให้เบราว์เซอร์เก็บไฟล์ไว้ (`public/.htaccess`) | CSS 230KB เหลือ ~30KB และเปลี่ยนหน้าครั้งต่อไปไม่ต้องโหลดซ้ำ |
| เพิ่ม index ให้ `sales` / `sale_items` (migration) | แดชบอร์ด รายงาน และ badge ค้างชำระ ไม่ต้องไล่อ่านทั้งตาราง |
| เลิกใช้ `DATE(created_at) = ...` เปลี่ยนเป็นเทียบช่วงเวลา | ทำให้ index ที่เพิ่มมาถูกใช้งานจริง |
| แดชบอร์ดเรียกสินค้าใกล้หมดครั้งเดียว (เดิม 2 ครั้ง) | ลด query ต่อการเปิดแดชบอร์ด |
| จอลูกค้าปรับจังหวะถามข้อมูลเอง (เดิมยิงทุก 1.5 วิ ตลอดวัน) | ลดคำขอช่วงร้านว่างราว 60-70% |
| หน้าจัดการสินค้าแบ่งหน้า หน้าละ 100 รายการ | ไม่ต้องวาดตารางเป็นพัน ๆ แถวทีเดียว (ร้านที่มีสินค้าไม่ถึง 100 จะไม่เห็นความต่าง) |
