# ที่อยู่ของไฟล์ latest.json สำหรับเช็คอัพเดทผ่านเน็ต
# ใส่ URL จริงเป็นบรรทัดล่างสุด (บรรทัดที่ขึ้นต้นด้วย # คือคอมเมนต์ ระบบข้ามให้)
#
# ตัวอย่างถ้าใช้ GitHub:
#   https://raw.githubusercontent.com/ชื่อผู้ใช้/ชื่อrepo/main/latest.json
#
# ยังไม่ได้ตั้งค่า = อัพเดทผ่านเน็ตไม่ได้ แต่ยังติดตั้งจากไฟล์ได้เสมอ:
#   .\update-pos.ps1 -Package "D:\pos-update-xxxx.zip"
